name: Check Format

on:
  push:
    branches:
      - main
  pull_request: {}

permissions:
  contents: read

jobs:
  check-format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Validate entry format and protocol counts
        run: python3 scripts/check_format.py
